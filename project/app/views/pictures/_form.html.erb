<% if false %>
<%= form_with(model: picture) do |form| %>
  <% if picture.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(picture.errors.count, "error") %> prohibited this picture from being saved:</h2>

      <ul>
        <% picture.errors.each do |error| %>
          <li><%= error.message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :图片, style: "display: block" %>
    <%= form.file_field :图片, :multiple => true %>
  </div>

  <div>
    <%= form.submit class:"click_button" %>
  </div>
<% end %>
<>